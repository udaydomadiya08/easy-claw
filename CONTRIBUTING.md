# Contributing to Easy Claw

PRs welcome. Keep it simple:
- One feature per PR
- Follow the existing file structure
- Test with `easy-claw setup` before submitting
- Skills go in `skills/`, hooks in `hooks/`, modules in `modules/`
